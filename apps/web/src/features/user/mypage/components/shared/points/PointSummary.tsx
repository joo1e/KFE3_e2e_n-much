// import { formatNumber } from 'src/shared/utils/formatNumber';

// interface PointSummaryProps {
//   amount: number;
//   balance: number;
// }

// const PointSummary = ({ amount, balance }: PointSummaryProps) => {
//   const pointColor = amount > 0 ? 'text-(--color-accent)' : 'text-(--color-red)';

//   return (
//     <div>
//       <p className="flex flex-col items-end text-xs">
//         <span className={`text-lg font-medium ${pointColor}`}>
//           {amount > 0 ? `+${formatNumber(amount)}P` : `${formatNumber(amount)}P`}
//         </span>
//         <span className="text-(--color-warm-gray)">잔액: {formatNumber(balance)}P</span>
//       </p>
//     </div>
//   );
// };

// export default PointSummary;
