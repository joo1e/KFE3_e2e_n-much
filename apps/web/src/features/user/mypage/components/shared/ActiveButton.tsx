// import { twMerge } from 'tailwind-merge';
// import { Button } from '@repo/ui/components/ui/button';

// type ActiveButtonProps = {
//   className?: string;
//   active?: boolean;
//   children?: React.ReactNode;
// } & Omit<React.ComponentProps<'button'>, 'className'>;

// const ActiveButton = ({ className = '', active = false, children, ...props }: ActiveButtonProps) => {
//   return (
//     <Button variant={active ? 'active' : 'inActive'} size="sm" className={twMerge(className)} {...props}>
//       {children}
//     </Button>
//   );
// };

// export default ActiveButton;
